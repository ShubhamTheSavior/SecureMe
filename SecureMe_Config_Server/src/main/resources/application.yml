server:
  port: 8888
spring:
  cloud:
    config:
      server:
        native:
          search-locations:
          - file:///C:/Spring_Boot_Workspace/Configuration_Properties
  profiles:
    active:
    - native
  security:
    user:
      name: config
      password: config
