spring:
  application:
    name: secure-me-metadata
  profiles:
    active:
    - dev
  config:
    import:
    - optional:configserver:http://config:config@localhost:8888
server:
  port: 0
eureka:
  client:
    service-url:
      ${EUREKA_URI:http://127.0.0.1:8888/eureka}
  instance:
    prefer-ip-address: true