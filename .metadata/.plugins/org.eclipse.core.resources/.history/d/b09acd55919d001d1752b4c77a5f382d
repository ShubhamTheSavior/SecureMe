spring:
  application:
    name: secure-me-metadata
  profiles:
    active:
    - dev
  config:
    import:
    - optional:configserver:http://config:config@localhost:8888
    
eureka:
  client:
    service-url:
      defaultZone: http://[myIP@]:8888/eureka
  instance:
    prefer-ip-address: true
    hostname: localhost